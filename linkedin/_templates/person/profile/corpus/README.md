Drop this person's raw source documents here (e.g. a LinkedIn profile export,
transcribed to plain-text markdown — not stored as a PDF/binary).

This is the corpus — untouched source text. `profile.md` is built from it;
nothing here gets duplicated into `profile.md`, only curated from it.
