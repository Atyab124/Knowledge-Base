Blank stamps for adding a new instance of something in this piece.

Currently: `person/` — copy it to `people/<name>/` to onboard a new person's
voice bundle. See `../CLAUDE.md` → "Adding a new person" for the steps.
