Each subfolder here is one person's complete, self-contained voice bundle —
nothing is shared between them. See `../CLAUDE.md` for the routing table and
`../_templates/person/` for the shape a new one starts from.
