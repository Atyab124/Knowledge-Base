Tooling used to build/maintain this repo's own structure — not a content piece,
not something an agent drafts from.

Currently: `icm-architect-and-frameworks/` — the vendored ICM Architect skill
(pinned to a specific upstream commit, checksum-verified) plus a private,
gitignored research snapshot. See its own `README-FIRST.md`.
